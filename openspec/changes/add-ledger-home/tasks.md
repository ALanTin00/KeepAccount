## 1. Data Layer

- [x] 添加 Room 相关依赖。
- [x] 定义账单记录 Entity。
- [x] 定义账单类型：支出、入账、不计入收支。
- [x] 定义账单分类字段和默认分类。
- [x] 添加 DAO 方法：新增账单、按月份查询、按类型查询、统计聚合。
- [x] 确保 App 不依赖任何网络请求也能完整运行。
- [x] 添加 2024 和 2025 两年本地测试账单数据填充。
- [x] 添加 Room 数据库版本升级和 Migration 示例。

## 2. Ledger Detail

- [x] 根据 `assets/main.png` 实现明细首页。
- [x] 显示当前选中的年月、总支出和总入账。
- [x] 按日期分组展示账单记录。
- [x] 根据 `assets/sel_day.png` 实现类型筛选弹窗。
- [x] 根据 `assets/tongji_riqi.png` 实现年月筛选弹窗。
- [x] 分类图标统一使用 `R.mipmap.ic_launcher`。
- [x] 明细列表支持每页 20 条分页加载。
- [x] 明细列表支持上拉加载更多。

## 3. Add Bill

- [x] 根据 `assets/add_data.png` 实现新增记账页面或弹窗。
- [x] 支持支出、入账、不计入收支三种账单类型。
- [x] 支持金额输入、分类选择、日期选择和备注。
- [x] 根据 `assets/add_note.png` 实现备注输入页面。
- [x] 根据 `assets/sel_mouth.png` 实现具体日期选择弹窗。
- [x] 点击确定后将账单保存到 Room。
- [x] 支持点击账单查看详情。
- [x] 支持从账单详情删除账单。
- [x] 支持从账单详情编辑账单。

## 4. Statistics

- [x] 根据 `assets/tongji1.png` 和 `assets/tongji2.png` 实现统计页。
- [x] 支持支出和入账切换。
- [x] 显示选中月份的总金额。
- [x] 显示分类占比图。
- [x] 显示分类排行。
- [x] 显示每日对比图。
- [x] 显示月度对比图。
- [x] 根据 `assets/tongji_详情.png` 实现统计分类详情页。

## 5. Validation

- [x] 验证关闭网络后 App 仍可正常使用。
- [x] 验证账单记录在 App 重启后仍然存在。
- [x] 验证月份筛选和类型筛选会更新明细列表和汇总金额。
- [x] 验证新增账单后明细页和统计页数据会更新。
- [x] 运行 `./gradlew.bat :app:assembleDebug`。

## 6. Settings

- [x] 设置页支持重新生成 2024 和 2025 测试数据。
- [x] 测试数据填充使用一次性标记，避免清空后重启自动恢复。
- [x] 设置页危险操作执行前显示二次确认。
