## 1. Statistics Data Preparation

- [x] 1.1 梳理统计页现有数据结构，确认当前月份记录、近几个月记录、分类汇总和图表输入来源。
- [x] 1.2 为每日对比生成按自然日汇总的图表项，包含日期、金额、是否当前选中。
- [x] 1.3 为月度对比生成按月份汇总的图表项，包含月份、金额、是否当前选中。
- [x] 1.4 为本月排行生成当前模式下的单条账单排行列表，按金额从高到低排序。

## 2. Category Donut Labels

- [x] 2.1 扩展分类占比圆环图绘制逻辑，计算每个扇区的中点角度和标签锚点。
- [x] 2.2 为扇区绘制引导线，并在引导线末端显示分类名称和占比。
- [x] 2.3 实现左右分栏和最小垂直间距处理，降低标签重叠和越界风险。
- [x] 2.4 保留分类排行列表点击进入分类详情的现有行为。

## 3. Daily Comparison Interaction

- [x] 3.1 为每日对比柱状图增加点击命中区域和选中状态。
- [x] 3.2 选中某一天后，高亮对应柱子并弱化非选中柱子。
- [x] 3.3 在图表顶部展示所选日期和金额提示。
- [x] 3.4 对提示浮层做左右边界钳制，确保靠近屏幕边缘时不超出可视区域。
- [x] 3.5 补齐每日对比横坐标日期标识和纵坐标金额刻度。

## 4. Monthly Comparison Interaction

- [x] 4.1 为月度对比柱状图增加点击命中区域和选中状态。
- [x] 4.2 选中某个月份后，高亮对应柱子并弱化非选中柱子。
- [x] 4.3 在月度对比顶部展示所选月份金额。
- [x] 4.4 月份或支出/入账模式切换后，重置或更新默认选中项。

## 5. Monthly Ranking List

- [x] 5.1 在统计页新增“本月排行”模块。
- [x] 5.2 排行列表展示排名、分类图标、分类名称、金额、日期时间和备注或辅助信息。
- [x] 5.3 支出模式金额显示为负数，入账模式金额显示为正数。
- [x] 5.4 当前年月和模式无记录时，不显示空排行列表。

## 6. Validation

- [x] 6.1 验证分类占比图标签和引导线在多分类数据下可读且不明显重叠。
- [x] 6.2 验证每日对比点击选中、提示金额和边缘浮层位置。
- [x] 6.3 验证月度对比点击选中、未选中弱化和顶部金额展示。
- [x] 6.4 验证本月排行按单条账单金额从高到低排序。
- [x] 6.5 验证支出/入账切换和年月切换后统计页全部模块刷新。
- [x] 6.6 运行 `.\gradlew.bat :app:compileDebugKotlin`。

## 7. Ranking Linkage Refinement

- [x] 7.1 月度对比选中月份变化时，排行列表切换到所选月份数据。
- [x] 7.2 排行标题展示当前排行对应的月份和模式。
- [x] 7.3 每日对比和月度对比点击命中只响应柱体高度范围。
- [x] 7.4 验证柱体外空白点击不会改变选中项或排行月份。
- [x] 7.5 运行 `.\gradlew.bat :app:compileDebugKotlin`。

## 8. Full Monthly Ranking

- [x] 8.1 统计页选中月份排行只展示前十条，并保留排名序号。
- [x] 8.2 在统计页排行底部增加“全部排行”入口。
- [x] 8.3 新增全部排行 Activity，Intent 传入年月和支出/入账模式。
- [x] 8.4 全部排行页展示返回入口、月份总金额、按金额/按时间切换和完整记录列表。
- [x] 8.5 全部排行页按金额从高到低、按时间从新到旧排序。
- [x] 8.6 验证 OpenSpec 严格校验和 Kotlin 编译。
## 9. Chart Edge Polish

- [x] 9.1 圆环图标签按文本宽度限制左右边界，避免长分类名被屏幕裁剪。
- [x] 9.2 每日对比 tooltip 三角形与气泡主体重叠绘制，消除边缘空隙。
- [x] 9.3 运行 `.\\gradlew.bat :app:compileDebugKotlin` 和 `openspec.cmd validate enhance-statistics-charts --strict`。
## 10. Daily Hit Area Polish

- [x] 10.1 扩大每日对比柱状图 x 轴点击响应区域，提升点击命中率。
- [x] 10.2 保持每日对比 y 轴响应区域仍限制在柱体高度内。
- [x] 10.3 运行 `.\gradlew.bat :app:compileDebugKotlin` 和 `openspec.cmd validate enhance-statistics-charts --strict`。